Well, uh
  [action-catch try-code <action-type-case:catch-code> <action-type-case:catch-code> <else:catch-code>]
    Handle arbitrary actions (exception ABORT)
  [action-finish action-type value]
    Can implement [return] [pass] [error] [break]
  {then}
    Like {do}, but it doesn't catch returns
  [if]
  [while]
